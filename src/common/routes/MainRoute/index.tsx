import { BrowserRouter } from 'react-router'
import { AppRouter } from '../index'
export const MainRoute = () => {
    return (
        <BrowserRouter>
            <AppRouter />
        </BrowserRouter>)
}
